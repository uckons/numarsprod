const db = require("../../config/db")

exports.list = async () => {
  const { rows } = await db.query(`
    SELECT
      id,
      name,
      address,
      is_active
    FROM branches
    WHERE deleted_at IS NULL
     ORDER BY id DESC
  `)
  return rows
}
